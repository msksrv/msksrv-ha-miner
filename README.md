# MSKSRV ASIC Miner — интеграция Home Assistant для ASIC-майнеров

**Язык:** **Русский** · [English](README.en.md)

**MSKSRV ASIC Miner** — пользовательская интеграция [Home Assistant](https://www.home-assistant.io/) для локального мониторинга и управления ASIC-майнерами. Данные получаются напрямую из локальной сети через [pyasic](https://github.com/UpstreamData/pyasic); облачный сервис не требуется.

Интеграция создаёт отдельное устройство для каждого майнера (Antminer, WhatsMiner, Avalon, Innosilicon, Goldshell, IceRiver, BitAxe и другие, поддерживаемые pyasic). Несколько устройств можно объединить в **ферму** — виртуальное устройство с общей статистикой, управлением пулами, аварийным отключением и необязательной **оценкой** стоимости электроэнергии.

[![GitHub Release](https://img.shields.io/github/v/release/msksrv/msksrv-ha-miner?style=for-the-badge)](https://github.com/msksrv/msksrv-ha-miner/releases)
[![License: Source-available](https://img.shields.io/badge/License-Source--available-red?style=for-the-badge)](LICENSE)
[![HACS](https://img.shields.io/badge/HACS-Custom-orange.svg?style=for-the-badge)](https://github.com/hacs/integration)

[![Откройте экземпляр Home Assistant и добавьте репозиторий в HACS.](https://my.home-assistant.io/badges/hacs_repository.svg)](https://my.home-assistant.io/redirect/hacs_repository/?owner=msksrv&repository=msksrv-ha-miner&category=integration)

---

## Содержание

- [Возможности](#возможности)
- [Установка](#установка)
- [Настройка](#настройка)
- [Ферма](#ферма)
- [Сущности майнера](#сущности-майнера)
- [Сущности фермы](#сущности-фермы)
- [Автоматизации](#автоматизации)
- [Службы](#службы)
- [Обнаружение](#обнаружение)
- [Требования](#требования)
- [Безопасность](#безопасность)
- [Устранение неполадок](#устранение-неполадок)
- [Сообщить об ошибке](#сообщить-об-ошибке)
- [Ограничения](#ограничения)
- [Скриншоты](#скриншоты)
- [Лицензия](#лицензия)

---

## Возможности

| Область | Что умеет |
|--------|-----------|
| **Обнаружение** | DHCP (имя хоста и MAC), сканирование подсети, ручной IP |
| **Майнер** | Хешрейт, температура, вентиляторы, пулы, лимит мощности, пауза майнинга |
| **Ферма** | Суммарный хешрейт и мощность, аварийное отключение, массовая смена пула, единый/двух- или трёхзонный тариф |
| **Автоматизация** | Службы и действия устройства для перезагрузки, режима работы и пулов |

Опрос — каждые **10 секунд**. При недоступности майнера сущности помечаются **недоступными** (без подстановки нулей).

---

## Установка

### HACS

1. **HACS → Интеграции → Пользовательские репозитории** (если ещё не добавлен):  
   `https://github.com/msksrv/msksrv-ha-miner`, категория **Integration**.
2. Установите **MSKSRV ASIC Miner** и перезапустите Home Assistant.

Либо нажмите кнопку HACS вверху этого README.

### Вручную

Скопируйте `custom_components/miner` в `config/custom_components/` и перезапустите Home Assistant.

---

## Настройка

Общий путь: **Настройки → Устройства и службы → Интеграции → MSKSRV ASIC Miner**.

Дальше в тексте: **«в интеграциях»** — этот раздел; **«⚙️ Настроить»** — пункт на строке записи майнера или фермы (не карточка устройства).

### Добавление майнера

1. **Добавить интеграцию** → выберите **Сканирование**, **Вручную** или завершите поток после DHCP-обнаружения.
2. При необходимости укажите учётные данные **RPC / веб / SSH**.
3. Задайте имя устройства.

### Перенастройка (reconfigure)

На записи одиночного майнера: **Перенастроить** (часто в меню **⋯**) — IP, учётные данные, диапазон мощности. Поля пароля оставьте **пустыми**, чтобы сохранить текущие значения.

### Опции майнера

**⚙️ Настроить** на записи майнера:

- **Выключатель питания** — привязка `switch` для кнопок включения/отключения питания.
- **Stratum-пул** — замена основного или добавление резервного (хост, порт, SSL, воркер). Перед применением запрашивается подтверждение; майнер должен быть доступен.

---

## Ферма

**Ферма** объединяет уже добавленные майнеры в одно виртуальное устройство.

1. **Добавить интеграцию → Ферма** → имя и список майнеров.
2. **⚙️ Настроить** на записи фермы открывает меню:

| Пункт | Назначение |
|-------|------------|
| **Состав фермы** | Список устройств-майнеров |
| **Датчики температуры** | `sensor` с `device_class: temperature` |
| **Пулы и воркеры** | До пяти пресетов stratum |
| **Тариф электроэнергии** | Единый или двух-/трёхзонный тариф |
| **Массовые действия** | Применить пресет ко всем участникам (с подтверждением) |

На каждом участнике через **⚙️ Настроить** задайте **выключатель питания** — иначе аварийная остановка для него недоступна.

**Аварийная остановка** последовательно выключает все привязанные выключатели питания. Ошибка одного выключателя не прерывает обработку остальных.

**Массовая смена пула** блокируется, если участники сообщают **разные алгоритмы**. Операция **не транзакционна**: если отдельный майнер недоступен или отклонит настройку, пул на других участниках к этому моменту уже может быть изменён.

**Тариф** — необязательная **оценка** стоимости по текущей мощности; это не учёт по счётчику (см. [Ограничения](#ограничения)).

После сохранения параметров фермы запись **перезагружается автоматически**.

Пароли пресетов в формах не показываются повторно; пустое поле пароля сохраняет текущее значение.

---

## Сущности майнера

| Платформа | Назначение |
|-----------|------------|
| **sensor** | Хешрейт, температура, пул, потребление, диагностика (IP, MAC, прошивка…) |
| **switch** | Пауза / возобновление майнинга (если поддерживается) |
| **number** | Лимит мощности (автотюнинг) |
| **select** | Режим питания; приоритет пулов (≥ 2 пулов) |
| **button** | Перезагрузка; **включение и отключение** питания через привязанный `switch` |

---

## Сущности фермы

| Платформа | Сущность | Описание |
|-----------|----------|----------|
| **sensor** | Суммарный хешрейт | TH/s по онлайн-участникам |
| **sensor** | Суммарная мощность | кВт |
| **sensor** | Майнеров / онлайн | Число участников и доступных |
| **sensor** | Алгоритмы | Сводка по онлайн-участникам; если никто не сообщает алгоритм — значение пустое |
| **sensor** | Эффективные чипы, % | По онлайн-участникам |
| **sensor** | Температура помещения | Привязанные датчики |
| **sensor** | Стоимость электроэнергии | При настроенном тарифе (час, сутки, месяц, накопительно, «сейчас») |
| **select** | Пресет пула | Какой слот применяют кнопки |
| **button** | Применить основной пул | Пресет → основной stratum у всех |
| **button** | Добавить резервный пул | Пресет → резервный слот |
| **button** | Аварийная остановка | Последовательное отключение питания |

---

## Автоматизации

`device_id` — из **Инструменты разработчика → Устройства**.

### Перезагрузка майнера

```yaml
action: miner.reboot
data:
  device_id:
    - YOUR_DEVICE_ID
```

### Смена основного пула

```yaml
action: miner.set_pool
data:
  device_id:
    - YOUR_DEVICE_ID
  mode: manual
  host: pool.example.com
  port: 3333
  use_ssl: false
  username: account.worker1
```

### Пул на всех участниках фермы

В воркере можно использовать `{ip}` или `{ip_last}`.

```yaml
action: miner.set_farm_pool
data:
  device_id:
    - FARM_DEVICE_ID
  mode: manual
  host: stratum.pool.com
  port: 443
  use_ssl: true
  username: user.{ip_last}
```

---

## Службы

| Служба | Описание |
|--------|----------|
| **`miner.reboot`** | Перезагрузка |
| **`miner.restart_backend`** | Перезапуск процесса майнинга |
| **`miner.set_work_mode`** | `low` / `normal` / `high` |
| **`miner.set_pool`** | Смена основного или добавление резервного пула |
| **`miner.set_farm_pool`** | То же для всех участников фермы |

**`miner.set_farm_pool`**: при разных алгоритмах у участников операция не выполняется. При частичном сбое (офлайн, отказ прошивки) часть майнеров может уже получить новый пул — см. [Ферма](#ферма).

`set_pool` / `set_farm_pool` доступны только как службы, не как действия устройства.

---

## Обнаружение

DHCP сопоставляет **имена хостов в нижнем регистре** (`whatsminer*`, `antminer*`…) и известные **MAC OUI** (Bitmain `E0A509`, WhatsMiner `C41025` / `C80831`). Интеграция опрашивает API до **3 раз** (таймаут 6 с). Уже настроенные майнеры пропускаются по IP, MAC и `unique_id`.

---

## Требования

- **Home Assistant** ≥ **2026.3.0** (см. `hacs.json`).
- **pyasic** `0.78.x` — устанавливается Home Assistant автоматически (`manifest.json`: `pyasic>=0.78.9,<0.79`).

Если в журнале **`Requirements for miner not found`**: проверьте доступ Home Assistant к **PyPI** и совместимость версии Python. Если ошибка сохраняется, приложите к [issue](https://github.com/msksrv/msksrv-ha-miner/issues) полный журнал установки.

---

## Безопасность

- Пароли майнеров (RPC, веб, SSH) и пулов хранятся в конфигурации Home Assistant.
- Пресеты пулов и их пароли попадают в **резервные копии** — защищайте бэкапы.
- API майнеров предназначен для локальной сети; не публикуйте их в интернет без необходимости.

---

## Устранение неполадок

| Симптом | Что проверить |
|---------|----------------|
| Майнер недоступен | Ping, пароли, файрвол, веб-интерфейс с хоста HA |
| Нет датчиков стоимости у фермы | Тариф в **⚙️ Настроить**; для зон — валюта и положительные цены во всех зонах |
| Массовая смена пула срывается | Журнал `Farm stratum:` — офлайн, разные алгоритмы, неверный пресет |
| DHCP не срабатывает | Шаблоны имён в `manifest.json` — в **нижнем регистре** |
| Ошибка установки pyasic | Сеть, прокси, Python; см. [Требования](#требования) |

---

## Сообщить об ошибке

[GitHub Issues](https://github.com/msksrv/msksrv-ha-miner/issues) — приложите:

1. Версию Home Assistant  
2. Версию интеграции (из **Интеграции → MSKSRV ASIC Miner → ⋮ → Система → Информация** или из релиза)  
3. Модель и прошивку майнера  
4. Версию pyasic (журнал загрузки)  
5. **Диагностические данные** интеграции (без паролей)  
6. Фрагмент журнала с ошибкой (без паролей и адресов пулов, если не нужны)

---

## Ограничения

- Набор сущностей и управление зависят от **pyasic** и прошивки конкретной модели.
- **Расчёт стоимости** — приблизительный, по периодически опрашиваемой мощности; не заменяет показания счётчика. Пропуски опроса и периоды недоступности не восстанавливаются.
- Отключение питания — только через привязанный HA `switch`, не через API отключения на стороне майнера.

Поддерживаемые типы устройств: [документация pyasic](https://pyasic.readthedocs.io/en/latest/miners/supported_types/).

---

## Скриншоты

Скриншоты форм настройки, меню фермы и карточки устройства — в каталоге [`docs/images/`](docs/images/) (если добавлены в репозиторий) или во [вложениях релизов](https://github.com/msksrv/msksrv-ha-miner/releases).

---

## Лицензия

Проект **source-available**, не open source — см. [`LICENSE`](LICENSE). Коммерческое использование — по предварительному письменному разрешению ([Issues](https://github.com/msksrv/msksrv-ha-miner/issues)).

**Благодарности:** [pyasic](https://github.com/UpstreamData/pyasic); форк идеи [hass-miner](https://github.com/Schnitzel/hass-miner) (Schnitzel, MIT).
